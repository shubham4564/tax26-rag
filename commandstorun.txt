# 0. Setup (everyone, day 1)
git clone <your-repo> && cd tax-rag
pip install -r requirements.txt
cp .env.example .env     # fill in OPENAI_API_KEY or ANTHROPIC_API_KEY

# 1. Person A — build the two corpora (days 1–7)
python -m ingestion.run_pipeline
# Output: data/chunks/flat_corpus.jsonl
#         data/chunks/hierarchy_corpus.jsonl

# 2. Person B — index both corpora (day 8, after A hands off)
python -m retrieval.build_index              # first time
python -m retrieval.build_index --rebuild    # if re-running from scratch
# Smoke test:
python -m retrieval.retrieve                 # runs built-in test queries

# 3. Person C — test pipeline then launch UI (day 9)
python -m generation.pipeline                 # quick smoke test (3 configs)
streamlit run app.py                          # full demo app

# 4. Person D — load dataset, run eval, analyze (days 7–13)
python -m evaluation.load_dataset             # download & save eval_set.jsonl
python -m evaluation.run_eval --dry-run       # test 5 questions first
python -m evaluation.run_eval                 # full run (takes 30–60 min)
python -m evaluation.analyze                  # comparison table + report